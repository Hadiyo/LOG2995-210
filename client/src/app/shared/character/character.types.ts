import { PlayerFacing, PlayerPose } from '@common/player/player.interface';

// Local aliases mapped to shared common game types (single source of truth).
export type CharacterState = PlayerPose;
export type CharacterDirection = PlayerFacing;
