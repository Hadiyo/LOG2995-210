export enum GameMode {
    CLASSIC = 'CLASSIC'
}

export enum MapSize {
    S = 10,
    M = 15,
    L = 20
}

export enum TileType {
    DIRT = 'DIRT',
    WATER = 'WATER',
    ICE = 'ICE',
    WALL = 'WALL',
    DOOR = 'DOOR',
}

export enum ObjectType {
    START = 'START'
}

export enum ObjectSize {
    S = '1x1',
    L = '2x2'
}